<form method="post" action="{{ path('app_relation_societe_adresse_delete', {'id': relation_societe_adresse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ relation_societe_adresse.id) }}">
    <button class="btn delete">Supprimer</button>
</form>
