{% extends 'base.html.twig' %}

{% block title %}RelationProjetSociete{% endblock %}

{% block content %}
    <h1>RelationProjetSociete</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ relation_projet_societe.id }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ relation_projet_societe.role }}</td>
            </tr>
            <tr>
                <th>Notes</th>
                <td>{{ relation_projet_societe.notes }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_relation_projet_societe_index') }}">back to list</a>

    <a href="{{ path('app_relation_projet_societe_edit', {'id': relation_projet_societe.id}) }}">edit</a>

    {{ include('relation_projet_societe/_delete_form.html.twig') }}
{% endblock %}
