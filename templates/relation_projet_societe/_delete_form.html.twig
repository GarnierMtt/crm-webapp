<form method="post" action="{{ path('app_relation_projet_societe_delete', {'id': relation_projet_societe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ relation_projet_societe.id) }}">
    <button class="btn delete">Supprimer</button>
</form>
