{% extends 'base.html.twig' %}

{% block title %} - Edit Societe{% endblock %}


{% block path %}
    <a href="{{ path('app_societe_index') }}">Sociétés/</a>
    <a href="{{ path('app_societe_show', {'id': societe.id}) }}">{{ societe.name }}/</a>
    <a href="{{ path('app_societe_edit', {'id': societe.id}) }}">Edit</a>
{% endblock %}
{% block content %}
    {{ include('societe/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('societe/_delete_form.html.twig') }}

{% endblock %}
